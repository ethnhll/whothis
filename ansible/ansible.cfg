[defaults]
nocows = True
roles_path = ./roles
inventory = inventory

# -n: never prompt. setup.sh's run_playbook grants a temporary NOPASSWD
# sudoers rule for the run, so become never needs a password; become over
# ansible_connection=local doesn't reliably handle its own password prompt.
[privilege_escalation]
become_flags = -n
